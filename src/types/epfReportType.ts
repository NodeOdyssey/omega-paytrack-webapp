// Define the type for each ESI report row
export type EPFRow = {
  accNo: string;
  empName: string;
  days: number;
  basicSalary: string;
  empEPF: number;
  emplrEPF: number;
  total: number;
  grandTotalEpf: number;
  totalBasicSalary: number;
};
